package bgu.spl.net.api.BGSSavedMessages;

import bgu.spl.net.api.BGSSavedMessage;

public class Post extends BGSSavedMessage {

    // constructor

    public Post(String senderUsername, String content) {

        super(senderUsername, content);
    }
}
